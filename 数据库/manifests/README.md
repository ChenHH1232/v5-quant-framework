# Database Manifests

Generated JSON manifests in this folder describe local data sources, schemas, warning counts, and agent access rules.

They are ignored by git because they describe local pulls that may change over time. Copy a manifest into `docs/` only when freezing a formal research packet.
